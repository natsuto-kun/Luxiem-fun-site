import * as React from "react"
import styled from "styled-components";
import { Header } from "../components/header";

export const Ike = () => {
    return (
        <div>
            <Header></Header>
            <p>Ikeの紹介ページ</p>
        </div>
    );
}