import * as React from "react"
import styled from "styled-components";
import { Header } from "../components/header";

export const Vox = () => {
    return (
        <div>
            <Header></Header>
            <p>Voxの紹介ページ</p>
        </div>
    );
}