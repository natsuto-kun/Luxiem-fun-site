import * as React from "react"
import styled from "styled-components";
import { Header } from "../components/header";

export const Shu = () => {
    return (
        <div>
            <Header></Header>
            <p>Shuの紹介ページ</p>
        </div>
    );
}